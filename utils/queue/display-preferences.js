/**
 * Queue Display Preferences
 * Manages how queues are displayed (menu vs chat mode)
 */

// Export the guild queue display preference map
export const guildQueueDisplayPreference = new Map();
